# Install packages
    # install.packages("MASS")
    # install.packages("fields")
    # install.packages("readxl")
    # install.packages("imager")
    # install.packages("plyr")
    # install.packages("clipr")
    # install.packages("readr")
    # install.packages("data.table")
# Load packages
# If you have these already, then you just need to load them.
library(data.table)
library(plyr)
library(MASS)
library(readxl)
library(imager)
library(clipr)
library(readr)
library(fields)


